

services:
  llmops-watcher:
    build: .
    container_name: llmops-watcher
    restart: unless-stopped
    working_dir: /app
    volumes:
      - ./prompts:/app/prompts
      - ./testing_data:/app/testing_data
      - ./configs:/app/configs
      - ./best_model:/app/best_model
      - ./best_prompt:/app/best_prompt
      - ./logs:/app/logs
    command: python main.py  # or python core/watcher.py
